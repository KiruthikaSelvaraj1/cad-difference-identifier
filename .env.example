# ============================================================
# CAD Review Studio — Environment Configuration
# ============================================================
# This project runs FULLY OFFLINE — no API keys are required.
#
# The rule-based summarizer generates natural language summaries
# entirely from computed statistics, with zero external API calls.
# This is a deliberate engineering decision for reliability, cost,
# and privacy. See README.md for the full rationale.
# ============================================================

# Backend server configuration (optional overrides)
# HOST=0.0.0.0
# PORT=8000

# Output directory for generated visualizations
# OUTPUT_DIR=./outputs
